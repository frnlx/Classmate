{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "start": "node start.mjs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build ./src/main.ts -o ./../../dist/server -m -s"
  },
  "nodemonConfig": {
    "watch": [
      "src"
    ],
    "ext": "js, json, ts, env",
    "ignore": [],
    "exec": "ts-node ./src/main.ts --transpileOnly --swc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
